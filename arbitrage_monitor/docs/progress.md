# Progress Board

> 更新时间: 2026-04-25
> 当前基线: `docs/requirements_codex_v1.md`

---

## 1. MVP Status

### 已完成

- 期指策略
- 转债策略
- 舆情策略
- 金属套利策略
- 期现溢价策略
- SQLite 存储
- cooldown 恢复
- Streamlit 看板
- Docker 部署骨架
- 基础集成测试

补充说明：

- 期指模块现已包含保证金快照、现货指数备源和 `16` 合约覆盖，且已补交割日后顺延边界测试
- 当前测试结果：默认 `python3 -m pytest -q` 为 `39` 通过；显式兼容套件 `python3 -m pytest -q tests/*.py` 为 `77` 通过
- 结构治理计划已建立，执行基线见 `docs/refactor_plan_20260423.md`
- 已接入 `A50 + Top10 合约加密资产池` 期现溢价模块，支持独立调度、SQLite 快照、参数页和看板
- 当前看板已切换为“分页查看 + 当前页手动刷新”的老 GUI 风格
- `alert_history.notified` 已接入“至少一个通知渠道发送成功”的本地状态回写
- 调度层已按策略拆分出独立 job，并为舆情增加独立低频扫描
- 调度层现已升级为“模块独立时钟 + GUI 可调”，并增加金属巡航/盯盘任务
- 共享运行参数已迁移到仓库内的 `config/runtime_settings.json`
- 金属阈值已支持在 GUI 中按品种编辑，并单独写入 `config/metals_thresholds.json`
- 已增加日度数据保留清理任务，默认保留最近 `14` 天报警历史、保证金快照、期指快照与金属快照
- 期指主表与金属主表已调整为固定业务顺序，前置字段按监控决策优先展示，其余字段保留在后
- 期指策略现已要求“贴水率阈值 + 年化贴水率阈值”同时满足才报警
- 已增加数据源健康度、配置变更审计和任务运行状态三类运维视图
- 高频快照已切到“分钟级去重 + 值变化才写 + 保底定时写入”，数据库增长速度已明显收敛
- `alert_history` 已改为同一标的全系统只保留最新一条报警
- 可转债链路已增加上市/退市状态过滤，未上市和已退市转债不会进入策略
- 舆情链路已补股票名称映射，报警与看板默认显示 `名称(代码)`
- 期指阈值已拆成 `IH / IF / IC / IM` 分品种配置，并支持共享基线与本机 local 覆盖
- 参数设置页已改成按模块折叠分组，模块开关、时钟和阈值现已集中到各自模块面板，并支持模块级单独保存
- 期现溢价阈值已支持按 `A50` 与 `加密资产 × 合约桶` 配置，并单独写入 `config/premium_thresholds.json`
- 期现溢价看板默认改为“快照优先 + 手动强制抓新”，并已修复 `Top10` 加密资产在快照回读时被旧结构压缩成 `BTC` 单资产的问题
- 调度器现已补齐可转债、舆情快照的持久化与历史清理统计，模块级快照链路保持一致

### 当前仍未闭环

| 序号 | 问题 | 负责人 | 优先级 | 触发条件/备注 |
|------|------|--------|--------|---------------|
| 1 | `alert_history.notified` 未细分多渠道送达状态与崩溃后补发语义 | 待定 | P2 | 通知链路增强 Phase 2 时处理 |
| 2 | 期指现货与保证金链路依赖外部站点 | 待定 | P3 | 已有备源，可接受 |
| 3 | SQLite 长期高压写入建议升级单写队列 | 待定 | P3 | 写入频率持续 > 100次/分钟时触发 |
| 4 | 金属 live 取数依赖外盘接口 | 待定 | P2 | fallback 已实现，需持续监控 |
| 5 | 加密资产 live 取数需更多交易所切换预案 | 待定 | P2 | Phase 2 通知链路增强时处理 |

> 注：当前所有模块已实现 fallback 机制，保证基本运行连续性。

---

## 2. Current Delivery State

| 模块 | 状态 | 说明 |
|------|------|------|
| 期指监控 | `DONE` | 已包含 `16` 合约覆盖、交割日后顺延边界测试、现货指数备源和保证金快照，见 `progress_ai_a_futures.md` |
| 转债监控 | `DONE` | 见 `progress_ai_b_convertible.md` |
| 舆情监控 | `DONE` | 见 `progress_ai_d_sentiment.md` |
| 金属套利 | `DONE` | 已接入金属 fetcher/strategy、GUI 阈值与独立时钟 |
| 期现溢价 | `DONE` | 已接入 A50 + Top10 合约加密资产池 fetcher/strategy、GUI 阈值、独立时钟与快照表，并补齐 Top10 快照回读与合约桶展示 |
| 调度和看板 | `DONE` | 当前看板已支持金属页和期现溢价页，调度已拆分为模块级独立 job，并统一落库期指/金属/溢价/可转债/舆情快照 |
| 通知传输 | `PARTIAL` | 真实发送与 `notified` 回写已接通，但多渠道细粒度回执与崩溃补发未完成 |

---

## 3. Historical Sub Logs

- `progress_ai_a_futures.md`
- `progress_ai_b_convertible.md`
- `progress_ai_d_sentiment.md`
- `progress_ai_integration.md`

说明：

- 这些文档是历史执行记录
- 若与当前主需求基线冲突，以 `requirements_codex_v1.md` 为准

---

## 4. Phase 2

以下仍属于规划，不计入当前交付完成度：

- 宏观策略
- 盈透策略
- 通知链路增强（失败重试 / 多渠道回执 / 崩溃补发）

---

## 5. Next Recommended Work

| 优先级 | 任务 | 预计工时 | 关联闭环项 |
|--------|------|----------|-----------|
| P1 | 加密期现链路补更多交易所切换预案 | 2d | #5 |
| P1 | 通知链路增强：失败重试 + 多渠道回执 + 崩溃补发 | 3d | #1 |
| P2 | 金属链路数据质量监控与失败告警 | 1d | #4 |
| P2 | 期指链路数据质量监控与失败告警 | 1d | #2 |
| P3 | SQLite 单写队列升级评估 | 2d | #3 |
| P3 | 宏观策略预研 | 待定 | - |

> 注：后续扩资产类别时，继续沿用”模块独立时钟 + 策略独立 job + 快照表”模式。
