# 文档改进建议

> 本文档记录了对现有文档体系的改进建议，按优先级排列。
> 2026-04-25 复核：本文档改为“建议台账”，每项必须标注 `TODO / DONE / OBSOLETE`，避免已完成建议继续冒充待办。
> 2026-04-26 复核：上一轮待办项已全部闭环；后续新增建议必须同时写明状态、落点和验证方式。

---

## 一、核心缺失（DONE）

### 1.1 关键文档缺席
- `requirements_codex_v1.md` 已存在，并在 `README.md`、`READ_ORDER.md`、`DOC_STANDARDS.md` 中被声明为当前事实来源。
- `api_registry.md` 和 `CONTRIBUTING.md` 已存在，仍需随代码变化持续维护。

### 1.2 建议
- [x] 补充完整的 `requirements_codex_v1.md`
- [x] 确保 A 级文档存在
- [x] 持续检查 A 级文档是否与最新代码状态对齐，并将该检查固化到 `DOC_STANDARDS.md` 的交叉一致性检查

---

## 二、dashboard_phase2.md 设计细节不足（DONE）

### 2.1 闭环结果
- `dashboard_phase2.md` 已补充异常态、空态、刷新策略、快速筛选、颜色语义和移动端可读定义。
- `dashboard_phase2.md` 已补充期指价差率颜色梯度和文字布局骨架。
- 本阶段不引入独立前端或高保真设计稿；若后续启动完整 UI 重构，再新增视觉规格。

### 2.2 建议补充
- [x] 异常态和空态的处理：快照为空、数据过期时界面显示什么
- [x] 刷新策略：每个视图的数据刷新频率
- [x] 快速筛选：筛选字段、交互方式（下拉/多选）
- [x] 颜色语义：哪些组件使用、期指贴水率的颜色梯度计算方式
- [x] 移动端可读定义：字号下限、横向滚动是否允许

---

## 三、各子进度文档老化（DONE）

### 3.1 当前问题
- `progress_ai_a_futures.md`、`progress_ai_b_convertible.md`、`progress_ai_d_sentiment.md`、`progress_ai_integration.md` 已同步到 `2026-04-25` 测试口径。
- 当前测试口径为：默认 pytest `49` 通过；兼容套件 `89` 通过。

### 3.2 建议
- [x] 更新所有子进度文档的 Current State Summary
- [x] 与 `progress.md` 的最新状态对齐

---

## 四、progress.md 未闭环项缺少信息（DONE）

### 4.1 当前问题
`progress.md` 当前仍未闭环表已经包含负责人、优先级和触发条件/备注。

### 4.2 示例
> "SQLite 写锁建议升级为单写队列"——什么时候触发这个升级？写入频率到多少？

### 4.3 建议
- [x] 为每条未闭环项添加：负责人、优先级、触发条件/预计解决时间
- [x] "下一步推荐工作"部分添加优先级顺序和估时

---

## 五、DEV_PROCESS.md 观察项缺少后续追踪（DONE）

### 5.1 闭环结果
- `DEV_PROCESS.md` 的观察项、仍未闭环项和下一步项已补充 `RESOLVED / DEFERRED` 状态。
- 已解决条目补充了解决日期链接或当前事实来源链接。
- 仍未进入当前 MVP 的条目改为 `DEFERRED`，并指向 `progress.md`、`api_registry.md` 或专题文档。

### 5.2 建议
- [x] 给每条观察项加"状态"字段（RESOLVED / DEFERRED）
- [x] 添加解决条目的日期链接或事实来源链接

---

## 六、DOC_STANDARDS.md 缺少可执行细节（DONE）

### 6.1 当前问题
- `DOC_STANDARDS.md` 已包含“较大变更”门槛、版本号机制、版本升级触发条件和 AI 正向指引。

### 6.2 建议
- [x] 定义"较大变更"的具体门槛
- [x] 添加文档版本号机制和版本迁移规则
- [x] 补充 AI 应该主动做什么的正向指引

---

## 七、金属套利和期现溢价缺少专题设计文档（DONE）

### 7.1 闭环结果
- 已新增 `docs/metals_arbitrage_design.md`。
- 已新增 `docs/premium_arbitrage_design.md`，并明确覆盖 `A50` 与 `crypto / 加密资产`。
- 两份专题文档已纳入 `README.md`、`READ_ORDER.md` 和 `DOC_STANDARDS.md`。

### 7.2 建议
- [x] 创建金属套利专题设计文档
- [x] 创建期现溢价专题设计文档

---

## 八、requirements_codex_v1.md 自身问题（DONE）

### 8.1 文档内部矛盾
- 已明确 `alert_history` 当前是“最新告警状态表 + 通知回写状态表”，不承担完整审计历史表职责。
- 已补充当前已落地的数据表和保留期清理口径。

### 8.2 期现溢价模块明显遗漏
- 已补期现溢价模块巡航/盯盘周期、交易时段和 `config/premium_thresholds.json` 配置说明。

### 8.3 版本未跟进实际演进
- 文档版本已更新到 `V1.3`。
- 测试基准已更新为默认 pytest `49` 通过、兼容套件 `89` 通过。
- 调度器演进已反映“模块独立时钟 + GUI 可调”。

### 8.4 策略需求细节不够可测试
- **BaseStrategy**：没有定义在哪个文件、包含哪些接口方法
- **金属策略 implied_rate**：没有解释语义
- **舆情策略 sentiment_pulse**：没有说明数据来源和计算方式

### 8.5 建议
- [x] 消除 `alert_history` 的矛盾规则，补全快照表
- [x] 补齐期现溢价的调度、时段、配置文件名三处空白
- [x] 做一次版本更新，同步 2026-03-15 之后的 Contract Change
- [x] 为各策略补充可测试的细节定义

---

## 优先级总结

| 优先级 | 问题 | 状态 | 预计工作量 |
|--------|------|------|----------|
| P1 | 完善 dashboard_phase2.md 异常态、空态、筛选和刷新细节 | DONE | 高 |
| P2 | 为 DEV_PROCESS.md 添加观察项状态追踪 | DONE | 低 |
| P3 | 创建金属套利 / A50 / crypto 期现溢价专题设计文档 | DONE | 高 |
| P3 | 持续检查 A 级文档是否与最新代码状态对齐 | DONE | 中 |
