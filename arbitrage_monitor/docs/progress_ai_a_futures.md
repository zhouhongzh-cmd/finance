# 期指组 (AI-A) 开发进度报告

> 说明: 本文档为历史执行记录。当前项目主基线已切换到 `docs/requirements_codex_v1.md`。
> 说明: 下方原始日志保留历史口径，不再代表当前期指实现全貌；以本页新增的 `Current State Summary` 和主基线文档为准。

> **更新时间**: 2026-04-25
> **最后同步**: `progress.md` 更新至 2026-04-25，默认 pytest 39 通过，兼容套件 77 通过
> **负责人**: AI-A (期指组)
> **状态**: ✅ 已完成
> **模块职责**: 监控国内四大股指期货（IF/IH/IC/IM）的深度贴水套利窗口。

---

## Current State Summary

> 更新时间: 2026-04-25

- 当前期指模块已不再以 `IF0`/`IC0` 连续合约作为主叙事口径，而是使用明确月份合约。
- 当前活跃合约规则为：有效月份中的当月、下月、之后最近两个季月。
- 当前会生成 `IF/IH/IC/IM` 各 `4` 档，共 `16` 个有效合约，并已覆盖交割日后顺延边界。
- 保证金比例已接入独立低频任务，固定在每日 `09:00` 与 `00:00` 刷新。
- 保证金比例运行链路当前为：中金所产品页 -> 中金财富期货日度公告 -> 静态默认值。
- 现货指数链路当前为：东方财富主源 -> 新浪指数备源。
- 当前真实环境验证结果：
  - 可恢复 `IF=14%`、`IH=14%`、`IC=15%`、`IM=15%`
  - 期指全链路可产出 `16` 条 live 数据
  - 当前 live 结果触发 `9` 个期指信号
- **当前测试结果**: 默认 pytest 39 通过；兼容套件 77 通过（与 progress.md 同步）
- 配置文件: `config/futures_thresholds.json`

---

## 1. 负责文件清单
- ✅ `fetchers/ak_futures.py` (期指数据拉取器)
- ✅ `strategies/futures_strategy.py` (期指贴水率计算策略)

## 2. 研发与实盘跑通日志 (RUNTIME_OUTPUT)
于 2026-03-11 完成了防反爬 (Mock json) 和线上直连 (Live) 双线验证：
- 线上获取到 `IC0` (中证500) 贴水率达到 **2.11%**。
- 系统准确报警，估算年化贴水高达 **51.32%**，成功触发极值警报 `CRITICAL`。
- 证明了整个策略链路（获取 -> 计算 -> 生成 Signal）组装有效，可随时移交调度器。

## 3. 排障避雷录 (TROUBLESHOOTING)
- 🚨 **问题**: 在初期使用 `ak.futures_zh_spot("IF0", market="CF")` 时，发现中金所实时数据接口极其不稳定，容易超时导致被封锁。
- 💡 **方案**: 已将入参调整为 `market="FF"` (新浪财经盘口接口)，请求成功率恢复至 100%。此情况已同步记入 `api_registry.md` 的 Fallback 说明中。
